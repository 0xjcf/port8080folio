{
  "name": "port8080folio-landing",
  "version": "1.0.0",
  "description": "Jose Flores - Technical Advisor Landing Page",
  "type": "module",
  "scripts": {
    "astro": "astro",
    "dev:astro": "astro dev",
    "build:astro": "astro build",
    "preview:astro": "astro preview",
    "dev": "tsc -p tsconfig.browser.json --outDir src --rootDir src --watch & serve src -l 8080",
    "serve:dist": "serve dist -l 8080",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "biome format --write src/",
    "lint": "biome lint --write src/",
    "check": "biome check --write src/",
    "check:ci": "biome check src/",
    "clean": "npx shx rm -rf dist",
    "copy:html": "tsx scripts/build-replace-vars.ts dev",
    "copy:html:prod": "tsx scripts/build-replace-vars.ts production",
    "copy:html:check": "tsx scripts/build-replace-vars.ts dev --dry-run",
    "copy:html:check:prod": "tsx scripts/build-replace-vars.ts production --dry-run",
    "copy:assets": "node -e \"import { access, cp, mkdir } from 'fs/promises'; import { constants } from 'fs'; (async () => { try { await access('src/assets', constants.R_OK); await mkdir('dist/assets', { recursive: true }); await cp('src/assets', 'dist/assets', { recursive: true }); } catch (e) { if (e.code !== 'ENOENT') { console.error(e); process.exit(1); } } })();\"",
    "build:js": "npx shx mkdir -p dist/scripts && esbuild src/scripts/pe.ts src/scripts/theme-toggle.ts src/scripts/analytics.js --bundle --minify --format=iife --target=es2017 --outdir=dist/scripts --entry-names=[name]",
    "build:css": "npx shx mkdir -p dist/styles && lightningcss --minify --bundle --targets \">=0.25%\" src/styles/main.css -o dist/styles/main.css",
    "build:html": "npx html-minifier-terser --input-dir dist --output-dir dist --file-ext html --collapse-whitespace --remove-comments --minify-css true --minify-js true",
    "build": "npm run build:prod",
    "build:dev": "npm run clean && npx shx mkdir -p dist && npm run copy:html && npm run copy:assets && npm run build:css && npm run build:js && npm run build:html",
    "build:prod": "npm run clean && npx shx mkdir -p dist && npm run copy:html:prod && npm run copy:assets && npm run build:css && npm run build:js && npm run build:html",
    "watch:css": "echo 'Tip: dev serves src/ directly; no CSS bundling/watch needed.'",
    "watch:js": "echo 'Tip: dev compiles TS -> JS via tsc --watch.'",
    "watch:html": "echo 'Tip: dev uses placeholders; dev-env.js sets endpoints locally.'",
    "test:lighthouse": "lighthouse http://localhost:8080 --output=json,html --output-path=./lighthouse-report --preset=perf --view",
    "test:fouc": "lighthouse http://localhost:8080 --output=json --output-path=./fouc-test --only-categories=performance --throttling.cpuSlowdownMultiplier=6"
  },
  "devDependencies": {
    "@astrojs/rss": "^4.0.14",
    "@astrojs/sitemap": "^3.5.1",
    "@biomejs/biome": "^2.2.0",
    "@cloudflare/workers-types": "^4.20240925.0",
    "@std/toml": "npm:@jsr/std__toml@^1.0.9",
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "astro": "^5.16.6",
    "esbuild": "^0.25.0",
    "happy-dom": "^20.0.2",
    "html-minifier-terser": "^7.2.0",
    "jsdom": "^24.0.0",
    "lightningcss-cli": "^1.30.1",
    "serve": "^14.2.5",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.32.0"
  },
  "engines": {
    "node": ">=18.17",
    "npm": ">=9"
  },
  "keywords": [
    "portfolio",
    "landing-page",
    "static-site"
  ],
  "author": "Jose Flores",
  "license": "MIT",
  "dependencies": {
    "valibot": "^1.1.0"
  }
}
